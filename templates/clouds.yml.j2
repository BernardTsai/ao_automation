clouds:
  {{vnfd.tenant.cloud}}:
    auth:
      auth_url:     "{{cloud.auth.auth_url}}"
      username:     "{{cloud.auth.username}}"
      password:     "{{cloud.auth.password}}"
      project_name: "admin"
    interface:     "admin"
    endpoint-type: "admin"
  {{vnfd.tenant.name}}:
    auth:
      auth_url:     "{{cloud.auth.auth_url}}"
      username:     "{{vnfd.tenant.name}}_administrator"
      password:     "{{vnfd.tenant.password}}"
      project_name: "{{vnfd.tenant.name}}"
    interface:     "public"
    endpoint-type: "public"
